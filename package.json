{
  "name": "blog-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "9um4 블로그 배포 웹훅 수신기 + build-once/pull-and-restart 배포 스크립트",
  "main": "bin/start.js",
  "scripts": {
    "start": "node bin/start.js",
    "test": "vitest run"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
